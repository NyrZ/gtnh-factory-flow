/**
 * The user-facing app version, shown as a chip in the header.
 *
 * Bump it with every merge to main so the live site is self-identifying:
 * minor for features (1.1.0), patch for fixes (1.0.1). Started at 1.0.0 the
 * day the equilibrium solver shipped (2026-08-02).
 *
 * The chip opens the changelog, so every bump needs an entry in
 * `src/lib/changelog.ts` written for players, not for developers.
 */
export const APP_VERSION = "1.17.0";
