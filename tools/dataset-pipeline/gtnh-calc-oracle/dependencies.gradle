// Track the GregTech the pack actually ships (GTNH 2.9.0-beta-2 loads
// gregtech-5.09.54.20.jar), so the symbols the exporter compiles against are
// the ones it reflects over at runtime. The older 5.09.52.258 pin no longer
// resolves at all: it drags in Thaumic_Exploration, whose pom asks for a
// CodeChickenLib:1.3.0 that GTNH's nexus has since unpublished.
dependencies {
    implementation("com.github.GTNewHorizons:GT5-Unofficial:5.09.54.20:dev")

    shadowImplementation("com.google.code.gson:gson:2.10.1")
}
